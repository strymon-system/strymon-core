pub struct Submitter {
    
}

impl Submitter {
    pub fn new(network: &Network, coord: addr) -> Result<Self> {
        
    }
    
    pub fn submit(&self, ) -> Box<Future<>>,

    pub fn topics(&self) -> Box<Future<Item=Vec<Topic>, Error=Io>> {
    
    }
}
