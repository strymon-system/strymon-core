use std::cell::RefCell;
use std::rc::Rc;

use async::queue::Receiver;

pub struct Coordinator {
    events: Receiver<Event, ()>,
}

impl Coordinator {
    pub fn new() ->  {
        Catalog {
            
        }
    }
    
    pub fn 
}
