Sessonization Benchmark
-----------------------

The main monolith is taken from trace_analizyer/analysis_stats and slightly
modified. In order to build this source tree, execute the following in the
current folder:

    git submodule update --init
    ../trace-analyzer/amadeus/logparse/third_party/build.sh
    cargo build --release

